module.exports = {
    name: 'hanna',
    description: "hanna",
    execute(message, args) {
        message.channel.send('A trebuchet is a mechanism that can launch a 90kg stone projectile over 300 meters POGGERS');
    }
}